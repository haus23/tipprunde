---
description: Create a new major prerelease version
---

Create a new major prerelease version using changelogen.

Run the following command:

```bash
pnpx changelogen --noAuthors --premajor pre --bump --release --push
```
