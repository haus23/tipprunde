---
description: Create and push a release version
---

Create and push a release version using changelogen.

Run the following command:

```bash
pnpx changelogen --noAuthors --bump --release --push
```
