---
description: Bump an existing prerelease version
---

Bump the existing prerelease version using changelogen.

Run the following command:

```bash
pnpx changelogen --noAuthors --prerelease --bump --release --push
```
