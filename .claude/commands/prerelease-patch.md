---
description: Create a new patch prerelease version
---

Create a new patch prerelease version using changelogen.

Run the following command:

```bash
pnpx changelogen --noAuthors --prepatch pre --bump --release --push
```
