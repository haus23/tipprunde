---
description: Create a new minor prerelease version
---

Create a new minor prerelease version using changelogen.

Run the following command:

```bash
pnpx changelogen --noAuthors --preminor pre --bump --release --push
```
