# Node Version and Package Manager

- Node Version

Die Node-Version wird auf die aktuelle LTS-Version eingestellt. Siehe `package.json`.

- Package Manager

Als Paketmaneger verwende ich ich der App `pnpm`. Siehe auch hierzu die `package.json`.
